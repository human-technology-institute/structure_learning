[build-system]
requires = ["setuptools >= 61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "structure_mcmc"
version = "0.1.0"
description = "Structure MCMC for graph structure learning in Bayesian networks."
requires-python = ">= 3.8"
dependencies = [
  "numpy",
  "pandas",
  "scipy",
  "scikit-learn",
  "matplotlib",
  "networkx",
  "igraph",
  "gmpy2"
]
readme = "README.md"
license = {file = "LICENSE"}
authors = [
    {name = "Kristopher Delas Peñas", email = "kristopher.delaspenas@uts.edu.au"},
    {name = "Roman Marchant", email = "roman.marchant@uts.edu.au"},
    {name = "Catarina Pinto Moreira"}
]
